{
  "name": "band-together",
  "version": "0.1.0",
  "description": "A band companion app for musicians",
  "private": true,
  "scripts": {
    "preinstall": "bun generate && cd api && bun reset && cd ../client && bun reset && cd ../shared && bun reset && cd ../db && bun reset",
    "postinstall": "rm -rf node_modules bun.lock package-lock.json",
    "generate": "cd api && bun run generate && cd ../db && bun run generate"
  },
  "keywords": [
    "band",
    "music",
    "setlist",
    "rehearsal",
    "gig"
  ]
}