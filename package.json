{
  "name": "band-together",
  "version": "0.1.0",
  "description": "A band companion app for musicians",
  "private": true,
  "scripts": {
    "preinstall": "cd shared && bun install && cd ../api && bun install && cd ../client && bun install && cd ../db && bun install",
    "postinstall": "rm -rf node_modules bun.lock package-lock.json"
  },
  "keywords": [
    "band",
    "music",
    "setlist",
    "rehearsal",
    "gig"
  ]
}