{
  "name": "band-together",
  "version": "0.1.0",
  "description": "A band companion app for musicians",
  "private": true,
  "scripts": {
    "preinstall": "bun clean && bun install:all",
    "install:all": "cd shared && bun reset && cd ../api && bun reset && cd ../client && bun reset && cd ../db && bun reset",
    "postinstall": "rm -rf node_modules bun.lock package-lock.json",
    "clean": "cd shared && bun clean && cd ../api && bun clean && cd ../client && bun clean && cd ../db && bun clean",
    "reset": "bun install"
  },
  "keywords": [
    "band",
    "music",
    "setlist",
    "rehearsal",
    "gig"
  ]
}