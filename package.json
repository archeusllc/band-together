{
  "name": "band-together",
  "version": "0.1.0",
  "description": "A band companion app for musicians",
  "private": true,
  "scripts": {
    "start": "docker compose up -d",
    "stop": "docker compose down",
    "nuke": "docker compose down && docker system prune -a -f --volumes",
    "submodule:update": "git submodule update --init --recursive --remote",
    "submodule:install": "cd db && bun install && cd ../api && bun install && cd ../client && bun install && cd ../cms-api && bun install && cd ../cms-client && bun install",
    "postinstall": "rm -rf node_modules bun.lock package-lock.json"
  },
  "keywords": [
    "band",
    "music",
    "setlist",
    "rehearsal",
    "gig"
  ]
}